{% extends "@!EasyAdmin/page/content.html.twig" %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-sm-6 col-md-4">
                {{ component('tv_show_chart') }}
            </div>
            <div class="col-sm-6 col-md-4">
                {{ component('episode_chart') }}
            </div>
        </div>
    </div>
{% endblock %}
